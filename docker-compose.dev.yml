services:
  client:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        CLIENT_PORT: ${CLIENT_PORT:3000}
    ports:
      - "${CLIENT_PORT:3000}:${CLIENT_PORT:3000}"
    volumes:
      - ./src:/app/src
      - ./lib:/app/lib
    tty: true
